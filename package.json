{
  "name": "task-worker",
  "version": "1.0.0",
  "description": "HTTP bridge service that accepts signed tasks from Hermes, forwards them to a local OpenClaw gateway, and posts AI results back to Hermes via webhooks.",
  "scripts": {
    "dev": "node --env-file=.env task-worker.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
